name: check

on:
  push:
    branches: [master]
  pull_request:

jobs:
  gates:
    name: gates (lint, types, tests)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          python-version: "3.12"
          enable-cache: true
      - name: Sync workspace
        run: uv sync --all-packages
      - name: Run gates
        run: make gates

  integration:
    name: integration (tools through the core)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          python-version: "3.12"
          enable-cache: true
      - name: Sync workspace
        run: uv sync --all-packages
      # Hermetic through-core integration tests only. Real provider scenarios are
      # opt-in e2e targets so CI stays reproducible.
      - name: Run integration tests
        run: make integration

  mutation:
    name: mutation (zero survivors)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          python-version: "3.12"
          enable-cache: true
      - name: Sync workspace
        run: uv sync --all-packages
      - name: Run mutation testing
        run: make mutation

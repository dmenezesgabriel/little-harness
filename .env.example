# Provider credentials, read from the environment by the litellm provider.
# Copy to `.env` and fill in only the keys you need; `.env` is gitignored.
# Full provider list: https://docs.litellm.ai/docs/providers
OPENAI_API_KEY=
GEMINI_API_KEY=
ANTHROPIC_API_KEY=
